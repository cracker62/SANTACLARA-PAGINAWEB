# Copia este archivo como .env (local) o crea estas variables en Vercel → Settings → Environment Variables.
# La clave se obtiene en https://console.anthropic.com  ·  NUNCA la pongas en archivos que se publican.
ANTHROPIC_API_KEY=
# Opcional: dominio desde el que se permite usar Olimpo (ej. https://monteolimpo.com.co)
OLIMPO_ORIGEN=
